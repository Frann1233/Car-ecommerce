const TEXT_VARIANTS = {
  heading: 'heading',
  subheading: 'subheading',
  regular: 'regular',
  detail: 'detail',
};

const DEFAULT_PAGINATION_SKIP = 0;
const DEFAULT_PAGINATION_TAKE = 12;

const DEFAULT_URL = 'https://mono-project.onrender.com';

const DEFAULT_EDIT_ICON = '✎';
const DEFAULT_DELETE_ICON = '✕';

export default {
  TEXT_VARIANTS,
  DEFAULT_URL,
  DEFAULT_EDIT_ICON,
  DEFAULT_DELETE_ICON,
  DEFAULT_PAGINATION_SKIP,
  DEFAULT_PAGINATION_TAKE,
};
